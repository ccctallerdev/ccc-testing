node_modules/
test-results/
playwright-report/
errors/
scripts/registro-talleres-resultado.json
# Evidencia que dejan los specs al fallar (capturas + HTML de cada etapa).
# Glob a propósito: cada spec nuevo estrena su carpeta y no hay que acordarse
# de venir a agregarla aquí.
scripts/debug*/
